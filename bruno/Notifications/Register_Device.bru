meta {
  name: Register Device (FCM Token)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/notifications/register-device
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "token": "fcm-device-token-from-flutter",
    "platform": "android"
  }
}
