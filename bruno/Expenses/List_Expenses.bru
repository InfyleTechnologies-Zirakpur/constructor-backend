meta {
  name: List Site Expenses
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/sites/:siteId/expenses
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  siteId: {{siteId}}
}
