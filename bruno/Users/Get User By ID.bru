meta {
  name: Get User By ID (Admin)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/users/:id
  body: none
  auth: bearer
}

params:path {
  id: USER_UUID_HERE
}

auth:bearer {
  token: {{token}}
}
