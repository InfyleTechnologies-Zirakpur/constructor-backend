meta {
  name: Deactivate User (Admin)
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/users/:id
  body: none
  auth: bearer
}

params:path {
  id: USER_UUID_HERE
}

auth:bearer {
  token: {{token}}
}
