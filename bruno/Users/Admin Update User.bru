meta {
  name: Admin Update User
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/users/:id
  body: json
  auth: bearer
}

params:path {
  id: USER_UUID_HERE
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "role": "contractor",
    "isActive": true
  }
}
