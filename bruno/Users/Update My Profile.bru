meta {
  name: Update My Profile
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/users/profile
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "fullName": "Updated Name",
    "phone": "+919876543210"
  }
}
