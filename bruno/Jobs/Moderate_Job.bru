meta {
  name: Moderate Job (Admin)
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/jobs/REPLACE_WITH_UUID/moderate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "status": "rejected",
    "moderationRemarks": "Violates terms of service: incorrect salary information."
  }
}
